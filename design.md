# Mosim — Design System (v2, 2026-06-01)

전제: **시니어 친화 + 따뜻한 신뢰(Warm Trust)**. 미국 은퇴 시니어가 "낯선 곳에서 혼자가 아니다"를 첫눈에 느끼게 하는 게 목표.
북극성: `docs/superpowers/specs/2026-06-01-mosim-product-definition.md`.

> **v1(마젠타 기반 K-Wellness)에서 전면 교체됨.** 옛 디자인은 `legacy-2026-06-01` 태그에 보존. 이 문서가 재구축의 단일 기준.

## 1. 디자인 원칙

1. **한 화면 = 한 메시지.** 첫 화면엔 헤드라인 하나·한 줄·버튼 하나. 여백을 크게. (애플식 미니멀 — 시니어가 한눈에 소화.)
2. **시니어 친화 ≠ 노인용 UI.** 큰 글씨·고대비·넓은 탭 타깃. 본문 19px 이상.
3. **사람 > 플랫폼.** 따뜻한 브랜드 목소리(감정·평온), himedi식 배지·후기·"Browse packages" 마켓플레이스 느낌 금지.
4. **AI는 보조, 사람이 책임.** AI 출력엔 "concierge confirms" 류 신뢰 회수 카피.
5. **신뢰는 차분하게.** 효율·속도 자랑(❌) 대신 "혼자 두지 않음"·"서두르지 않음"(✅).

## 2. 컬러 — Warm Trust 팔레트

```css
--ivory:      #F7F2EA;   /* 기본 배경 (따뜻한 아이보리) */
--ivory-soft: #FBF8F2;   /* 카드·옅은 면 */
--navy:       #1B2A4A;   /* 헤드라인·메인 텍스트·1차 버튼 */
--navy-soft:  #2A3C5E;   /* 약한 강조, 이탤릭 */
--gold:       #C39A3F;   /* 액센트 — 밑줄·라벨·구분점 (절제해서) */
--gold-soft:  #D9BD7E;   /* 옅은 골드 */
--ink:        #54514B;   /* 본문 (따뜻한 그레이브라운) */
--ink-2:      #8A8479;   /* 보조 텍스트 (이보다 옅게 금지) */
--line:       #E5DBC8;   /* 구분선·테두리 */
```

- 핑크/마젠타 **폐기**. 골드는 **액센트로만**(밑줄·라벨·구분점), 면으로 넓게 깔지 않기.
- 다크 면이 필요하면 `--navy` 기반(검정 금지).

## 3. 타이포그래피

**글꼴 — 2종만**
```css
/* 디스플레이·헤드라인·브랜드: 따뜻한 세리프 */
font-family: 'Lora', Georgia, serif;
/* 본문·UI·라벨: 깨끗한 산세리프 */
font-family: 'Inter', -apple-system, system-ui, sans-serif;
```
- 헤드라인은 **Lora serif**(따뜻함·격·차별화 — 경쟁사 지오메트릭 산세리프와 갈라치는 핵심). Google Fonts로 `Lora:wght@400;500;600` 로드, Georgia 폴백.
- 본문은 **Inter**, **19px 이상**(시니어 최소).

**스케일**

| 클래스 | 크기 / 글꼴 | 용도 |
|---|---|---|
| `.display` | clamp(40px, 6vw, 60px) / Lora 600 | Hero h1 |
| `.h2` | clamp(30px, 3.6vw, 44px) / Lora 600 | 섹션 헤더 |
| `.h3` | 19~22px / Lora 600 | 카드 제목 |
| `.lede` | 20px / Inter 400 | 부제 |
| `.body` | 19px / Inter 400, line-height 1.6 | 기본 본문 |
| `.label` | 12.5px / Inter 600, letter-spacing 2.4px, UPPERCASE, 골드 | 섹션 라벨(eyebrow) |

## 4. 컴포넌트

### 4.1 버튼
- **1차:** `--navy` 채움, 흰 글자, radius 10px, padding 16×30, 16px/600. (시니어 탭 타깃 ≥ 44px 보장.)
- **텍스트 링크:** `--navy` 글자 + `--gold` 밑줄 2px("See how it works →"). CTA 대용 금지.
- **고스트(nav):** 골드 1.5px 보더 pill.

### 4.2 사진
- 따뜻한 돌봄·한국의 장면(스톡 클리닉 사진 ❌). radius 16~20px, 부드러운 그림자.
- 라이선스 확보 전 `<image-slot>` placeholder 유지.

## 5. 레이아웃

- 컨테이너 `.wrap` 1080px, padding 40px.
- 섹션 상하 리듬 **여유 있게**(64~112px). 비우는 걸 두려워하지 말 것.
- Hero는 가운데 정렬·단일 컬럼·큰 여백. 정보는 스크롤 따라 점진 공개.
- nav: 얇게(브랜드 + 링크 3~4 + CTA 1). sticky 가능.

## 6. 보이스 (카피 톤)

- 영어, 미국 시니어가 읽기 쉬운 따뜻하고 차분한 문장.
- 핵심 약속: **"You're never alone in Korea."** 감정 우선, 거래 표현 자제.
- 의료는 쉬운 말로(겁나는 의학용어 ❌).

## 7. 디자인 변경 시 체크

- [ ] 본문 19px 이상인가?
- [ ] 회색이 `--ink-2`보다 옅지 않은가?
- [ ] 탭 타깃 44×44 이상인가?
- [ ] 핑크/마젠타 안 썼는가? (폐기됨 — 골드 액센트만)
- [ ] 헤드라인에 Lora serif 썼는가?
- [ ] 첫 화면이 "한 메시지"로 비워졌는가? (정보 과다 ❌)
- [ ] himedi식 배지·마켓플레이스 느낌 아닌가?
